export { PrismaClient, Prisma } from '@prisma/client';
export type { UserRole } from '@prisma/client';

// Domain model types
export type {
  User,
  RefreshToken,
  AuthAttemptLog,
  Region,
  Device,
  Schedule,
} from '@prisma/client';
